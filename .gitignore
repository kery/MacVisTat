*.pro.user
build/*
!build/pcre.dll
# pdb file is needed to debug program with dump file on Windows
!build/*.pdb
# sym file is, which is similar to pdb file on Windows platform, generated by dump_syms tool on
# linux platform
!build/*.sym
